# 函数专题(for Julie)

## 变量作用域

假定你要写一个函数，来解决一个问题。

这个函数类似一个数学函数：你给他输入（参数），就得到一个结果（返回值），其他都不重要。

例如，我们写一个加法函数，把2个变量相加。

```{python}
def add(x, y):
  return x + y

add(1, 2)
```

注意到，这个函数体内定义了2个变量，`x`和`y`。在函数体的内部，`x`和`y`的名字就会覆盖